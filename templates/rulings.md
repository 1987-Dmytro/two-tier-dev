# Rulings — `docs/reviews/<date>-plan-<phase-name>.md` — the decisions log of a phase (team-lead file)

<!-- A ruling DECIDES; it never legislates. ≤12 lines. It names a choice, a number the instrument produced
     (with the command that produced it), or a file to read. It never adds a test, pin, guard or ledger.
     Appended in order; letters (a), (b), … The executor reads the NEWEST dated section every session.
     A ruling that must carry commands is a sign the project's PROCESS.md lacks a default — write it there. -->

## Ruling <date> (<letter>) — <one line: what was decided, in the operator's terms>

1. **Accepted:** `<sha>` … — <what the diff proved, the check that was re-run by the team lead and what it
   printed, the numbers with their files>. <What was hunted for: scope silently added or dropped.>
2. **The stop / the reading:** <class: planned read · fork · defect · money · process> — <the root, in one
   sentence; who could have foreseen it: the phase file (then it is the team lead's) or nobody>.
3. **The decision:** <the choice> — <the number the instrument produced and the command whose output it is>,
   <the file to read>. Money: <cap quoted from the dry run that priced it; the line it is registered under>.
4. **What enters where, the same day:** the phase file (v<k>, §<n>) · PROCESS.md (a mechanic) · the skill card
   (a pattern) · the executor's harness. <Nothing else moves.>
5. **Next session = ONE item:** <its name and its check>; the standing prompt is unchanged.

### Addendum (<letter>)+, <date> <time> — <when a ruling needs a correction the same day, append, never rewrite>
<≤12 lines; names what the original ruling got wrong and whose miss it was>

<!-- The stop review for this ruling goes to docs/reviews/<date>-stop-patterns.md (≤5 lines per stop, one file
     per day) — see templates/stop-patterns.md. -->
