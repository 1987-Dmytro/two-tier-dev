# Stop patterns — `docs/reviews/<date>-stop-patterns.md` — one file per day, ≤5 lines per stop (team-lead file)

<!-- The self-improvement pass (skill §10), written after EVERY ruling, never skipped. A planned read (an error
     table the team lead must judge) is not a failure and is not counted as preventable; a fork or a defect the
     catalogue could have answered IS — and so is a question inside a planned stop that the phase file could
     have answered. A verifier's finding BEFORE a purchase is a stop that did not happen: same pass. -->

## <n>. <ordinal> stop of the day — (<ruling letter>), s<session>: <one line: what stopped>
| # | stop | class | root | foreseeable by the phase file? | what entered, and where |
|---|---|---|---|---|---|
| (<letter>) | <what the executor wrote as the open stop, with the number or the refusal text> | planned read · fork · defect · money · process | <the root in one sentence: which rule was missing, typed, borrowed, or read from prose> | yes — the team lead's (<which section could have held it>) · no · half (<which half>) | PHASE v<k> §<n>: <the default> · PROCESS.md «<section>»: <the mechanic> · skill v<x> §<n>: <the pattern> · harness: <what> |

Day's tally: <sessions> sessions, <stops> stops (<preventable> preventable — whose), <planned reads> planned reads.
Diet signal: <which retro threshold this day crossed, if any, and what the next session is instead of a feature>.

<!-- The operator is told in three lines: the pattern · what changed · what it prevents. A class seen twice
     becomes a default in the fork catalogue; a default that never fired in a phase is deleted at the retro. -->
